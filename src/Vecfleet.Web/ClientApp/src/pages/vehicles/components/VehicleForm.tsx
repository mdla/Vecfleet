import {Form} from "react-bootstrap";

interface Props {

}

const VehicleForm = (props: Props): JSX.Element => {

    return (
        <Form>
            Formualrio
        </Form>
    );
};
export default VehicleForm;
